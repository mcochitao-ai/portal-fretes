services:
  - type: web
    name: portal-fretes
    env: python
    plan: free
    buildCommand: pip install --upgrade pip && pip install -r requirements.txt && python manage.py collectstatic --noinput && python manage.py migrate && python manage.py init_deploy
    startCommand: gunicorn app:app -c gunicorn.conf.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: False
      - key: ALLOWED_HOSTS
        value: portal-fretes.onrender.com
      - key: DATABASE_URL
        fromDatabase:
          name: portal-fretes-db
          property: connectionString
    healthCheckPath: /
  - type: pserv
    name: portal-fretes-db
    env: postgresql
    plan: free
    ipAllowList: []
